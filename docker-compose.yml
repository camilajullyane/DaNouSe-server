services:
  dns-server:
    build:
      context: .
    environment:
      DNS_REGISTER_TOKEN: ${DNS_REGISTER_TOKEN:-change-me}
      DATABASE_URL: file:/app/data/dev.db
    ports:
      - "5300:5300/udp"
    volumes:
      - dns-data:/app/data
    restart: unless-stopped

volumes:
  dns-data:
